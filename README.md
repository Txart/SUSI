# Install SUSI

We use [`uv`](https://docs.astral.sh/uv/getting-started/installation/) to set the environment and manage packages.

You can run:

```bash
uv sync
```
to install all the packages.

But they will be installed when you run the application anyways:
```bash
uv run <command_name>
```
